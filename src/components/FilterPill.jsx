import reat from "react";

const FilterPill = () => {
  return (
    <div className="flex flex-col gap-y-9 h-fit z-10 absolute left-3 top-[40%] rounded-full w-fit bg-[color:var(--filter-bg--color)] border-2 border-[color:var(--user-icon--bg-color--purple)] p-4">
      <div>Icon</div>
      <div>Icon</div>
      <div>Icon</div>
    </div>
  );
};

export default FilterPill;
